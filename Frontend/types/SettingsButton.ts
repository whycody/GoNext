export type SettingsButton = {
    id: number;
    name: string;
    description: string;
    color: string;
    icon: string;
  }
  
  export type SettingsButtonModel = {
    id: number;
    name: string;
    description: string;
  }